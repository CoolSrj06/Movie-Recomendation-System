# Copy this file to `.env` and replace the placeholder with your TMDB API Key (v3 auth).
# Do NOT commit your real `.env` file — it is included in `.gitignore`.

TMDB_API_KEY=your_tmdb_v3_api_key_here
